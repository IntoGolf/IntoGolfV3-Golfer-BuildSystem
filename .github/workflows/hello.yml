name: Hello World Test
on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - name: Say hello
      run: |
        echo "Hello from steble_design branch!"
        echo "GitHub Actions is working! 🎉"
        date